import React, { useEffect, useState } from "react";
import {
  ActivityIndicator,
  FlatList,
  Text,
  View,
  TouchableOpacity,
  RefreshControl,
  SafeAreaView,
  Linking,
} from "react-native";
import { Icon, Image } from "react-native-elements";

function TeamPlayersScreen({ route, navigation }) {}

export default TeamPlayersScreen;
